This directory holds the Segment Anything checkpoint `sam_vit_h_4b8939.pth` (about 2.5 GB).

The file is not stored in git to keep the repository lightweight. Download it prior to running the pipeline:

```
bash download_models.sh
```
